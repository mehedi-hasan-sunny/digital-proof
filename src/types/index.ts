export interface InjectFileListType {
    value: Array<File> | FileList
}

export interface CanvasSelectedOptions {
    bleedSize: number,
    showFoldedArea: string
}

export enum YesOrNo {
   Yes= "yes",
   No= "no"
}